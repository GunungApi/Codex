name: P011 production bootstrap
on:
  push:
    paths: ['.github/workflows/p011_bootstrap_20260923.yml']
permissions:
  contents: read
jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - name: Prepare Python 3.13 renderer wheels and temporary transfer key
        run: |
          mkdir -p wheels private public
          python -m pip download --dest wheels --only-binary=:all: --python-version 313 --implementation cp --abi cp313 --platform manylinux2014_x86_64 moderngl==5.12.0 glcontext==3.0.0
          openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:3072 -out private/key.pem 2>/dev/null
          openssl pkey -in private/key.pem -pubout -out public/key.pub
          cat public/key.pub
      - uses: actions/upload-artifact@v4
        with:
          name: p011-renderer-wheels
          path: wheels/
          retention-days: 1
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        with:
          name: p011-transfer-key
          path: private/key.pem
          retention-days: 1
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        with:
          name: p011-public-key
          path: public/key.pub
          retention-days: 1
          if-no-files-found: error
